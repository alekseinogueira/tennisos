# Progress

> What works, what's left, known issues. Updated at the end of every session via `/umb`.
> Read this first at the start of every task.

## What Works
- Context infrastructure (CLAUDE.md, memory-bank, slash commands, explorer agent).
- Read-only server/codebase audit (`audit-report.md`) and EPC reuse review (`reuse-epc.md`).
- Architecture design (`architecture.md`) and database blueprint (`database-blueprint.md`).

## In Progress
- Nothing active. Design phase complete; awaiting the build session.

## Not Started
- App scaffold (Vite + React project files).
- EPC plumbing copy (`lib/supabase.js`, `lib/db.js`).
- Auth layer (AuthProvider, ProtectedRoute, RoleRoute, auth screens).
- Supabase migrations (profiles+auth, MVP schema, RLS policies).
- Coach/student screens; 55TC theming.

## Known Issues
- None yet.
